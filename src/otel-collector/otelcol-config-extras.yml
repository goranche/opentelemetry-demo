# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

# extra settings to be merged into OpenTelemetry Collector configuration
# do not delete this file

## Example configuration for sending data to your own OTLP HTTP backend
## Note: the spanmetrics exporter must be included in the exporters array
## if overriding the traces pipeline.
##
#  exporters:
#    otlphttp/example:
#      endpoint: <your-endpoint-url>
#
#  service:
#    pipelines:
#      traces:
#        exporters: [spanmetrics, otlphttp/example]



exporters:
  otlphttp/signoz:
    endpoint: http://host.docker.internal:4318
    tls:
      insecure: true

service:
  pipelines:
    traces:
      exporters: [otlp, debug, spanmetrics, otlphttp/signoz]
    metrics:
      exporters: [otlphttp/prometheus, debug, otlphttp/signoz]
    logs:
      exporters: [opensearch, debug, otlphttp/signoz]




# exporters:
#   otlp/signoz:
#     endpoint: http://host.docker.internal:4317
#     tls:
#       insecure: true
#
# service:
#   pipelines:
#     traces:
#       exporters: [otlp, debug, spanmetrics, otlp/signoz]
#     metrics:
#       exporters: [otlphttp/prometheus, debug, otlp/signoz]
#     logs:
#       exporters: [opensearch, debug, otlp/signoz]
